// @flow 
import * as React from 'react';
import Layout from "@/Layout/Layout";

export default function NotFound () {
    return (
        <Layout>
            <div>Not found</div>
        </Layout>
    );
};
