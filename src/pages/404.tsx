// @flow 
import * as React from 'react';
import { Layout } from "@/Layout/Layout";

function NotFound () {
    return (
        <Layout>
            <div>Not found</div>
        </Layout>
    );
}

export default NotFound
